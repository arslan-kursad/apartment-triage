## Day 16 — 29 May 2026 (Cumartesi)

### Bugün yapılanlar
- POST handler Forbid() → StatusCode(403) fix
- pgvector InvalidCastException fix (double cast)
  → EnricherAgent similarity search artık çalışıyor
- Classifier prompt v3 (P1 pattern iyileştirmesi)
- RouterAgent Rule 3+4 kombinasyon fix
  (Urgent + MissingLocation edge case)
- Edge case mining

### Keşke önceden bilseymişim

GÖZLEM 1 — Prompt-as-Code:
  Prompt'ları C# const string içinde tutmak
  başlangıç için hızlı. Ama v2→v3 geçişinde
  "hangi satır ne zaman eklendi?" güçleşti.
  Yeniden yaparsam: prompts/classifier/v3.md,
  manifest.yaml aktif versiyon. Prompt PR
  ≠ kod PR. ADR-0005'te planlıydı, const string
  kaldı — implementation gap.

GÖZLEM 2 — Rule Engine:
  5 hardcoded if-else şu an yeterli.
  Ama Rule 3 + Rule 4 kombinasyonu
  (Urgent + MissingLocation) "Rule 3 içinde
  MissingLocation kontrolü" gerektirdi —
  kısa vadeli fix. Rule sayısı 8-10'a çıkarsa
  chain of responsibility kaçınılmaz.
  Her rule ayrı sınıf, ayrı test: IRoutingRule.

GÖZLEM 3 — Embedding Pipeline ROI:
  Similarity-based routing (Rule 5) production'da
  hiç ateşlenmedi. 11 ticket, hepsinde
  has_embedding=False (dev ortamı fallback).
  pgvector fix sonrası path artık temiz.
  Gerçek trafik bekleniyor.
  "Infrastructure feature'ların ROI'si
   trafik olmadan görünmüyor."

### Loom Q5 kullanım planı (Architect'ten)
  1. ClassifierAgent.cs → prompt C# içinde
  2. RouterAgent.ApplyRules() → P1 fix hikayesi
  3. pgvector cast bug → embedding pipeline
  90 saniye, "ne eksik biliniyor" = engineering maturity.

### Phase 3 sayaç
  KVKK disclosure gönderildi (bugün)
  Gerçek sakin mesajı bekleniyor
  Embedding artık aktif → ilk similarity match gelecek

### Yarın (Day 17)
  Gerçek mesaj izleme
  Similarity routing ilk ateşlenme takibi
  Prompt v3 eval re-run
