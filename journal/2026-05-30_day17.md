## Day 17 — 30 May 2026 (Cumartesi)

### Bugün yapılanlar

ADR-0012 + Dashboard Modernization — 3 faz, 3 commit

**FAZ 1 — Tailwind Shell + Genel Bakış:**
- Bootstrap → Tailwind CDN + Flowbite + Chart.js
- Fixed sidebar (mobile hamburger), Istanbul saati (JS + server-side helper)
- TR/EN toggle (localStorage, span.lang-tr/lang-en pattern)
- Genel Bakış (`/`): 4 KPI kart, canlı triage feed, 6 Chart.js chart
  (kategori, kanal, şiddet, yönlendirme, model kalitesi, FinOps)
- Stats endpoints: GET /api/stats, /categories, /severity, /routing,
  /trends (14-gün), /api/tickets/recent, /api/eval/summary
- appsettings `Dashboard:Eval` section (eval run sonuçları elle girilir)

**FAZ 2 — Tickets Tailwind Port:**
- Tickets Index: kanal dot (#25D366 WA / #229ED9 TG), raw_text preview,
  Resident.DisplayName, Istanbul saat
- Tickets Detail: emergency banner, bilingual labels, clean Razor
- TicketRepository.GetPagedAsync: SourceMessage + Resident eager load eklendi

**FAZ 3 — Inbox + HITL:**
- /inbox: 3-panel (list 176px + chat + triage panel 200px)
- Draft reply: ReplyTemplates.BuildTicketReply() (lang-aware, no schema change)
- POST /api/messages/{ticketId}/reply → IMessageChannel keyed service → SendAsync
- WA: Meta test kısıtı nedeniyle infra hazır, prod'da aktif

### Açık karar (FLAG edildi)

ADR-0012 §Açık Sorunlar:
FinOps Haiku/Sonnet oranı için Ticket entity'sinde EscalatedToSonnet (bool)
gerekiyor. Schema migration = Cannot Decide. Dashboard şimdilik "N/A — pending".

### Keşke önceden bilseymişim

GÖZLEM 1 — Razor nested @{} tuzağı:
  Detail.cshtml'de `@{}` içine `@{}` yazınca
  RZ1010 parse hatası. C# kodu ayrı blokta, HTML ayrı —
  karıştırma: tüm değişkenleri top-level `@{}` bloğuna taşı.

GÖZLEM 2 — Eval fixture count vs spec:
  Spec "38 etiketli test case" diyor; gerçek:
  15 main + 33 edge case = 48 toplam. Spec eski versiyona
  işaret ediyordu. `/api/eval/summary` 48 döner.

### Yarın (Day 18)

Prompt v3 eval re-run → Dashboard:Eval config güncelle
Gerçek mesaj izleme → similarity routing ilk ateşlenme
EscalatedToSonnet migration onayı bekliyor (Architect'ten)
