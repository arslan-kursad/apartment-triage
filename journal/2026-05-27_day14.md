# Day 14 — 27 Mayıs 2026 (Çarşamba)

## Bugün yapılanlar

- **Telegram UX 3 katman:** `/start` karşılama, ticket sonrası geri dönüş, clarification format.
- **Clarification flow + re-triage:** İkinci mesaj sonrası ticket güncelleniyor. `CategoryAmbiguous` varsa full pipeline yeniden çalışıyor (`ReclassifyTicketAsync`).
- **Image input:** Görsel mesaj DB'ye persist ediliyor, dashboard ticket detayında görünüyor.
- **Classifier prompt v2:** Electrical kategorisi few-shot + NonActionable constraint ile iyileştirildi.
- **Router guard:** `EmergencyConfidence=High` iken `NonActionable→Archive` engellendi.
- **ec-0032, ec-0033** eval suite'e eklendi — 4/4 smoke PASS.
- **ADR-0011** yazıldı: Image ✅ / Voice ❌ / Video ❌ kararları formalize edildi.

## Commits (ccb174f → e1839ca)

| Hash | Konu |
|---|---|
| `ccb174f` | feat(orchestrator): re-triage on clarification if CategoryAmbiguous |
| `0741d46` | fix(prompts): classifier v2 — electrical few-shot + NonActionable guard |
| `a66072b` | fix(prompts): router v2 — high emergency confidence archive prevention |
| `5e5cc9c` | test(eval): ec-0032, ec-0033 — electrical classification cases |
| `e1839ca` | docs: ADR-0011 multimodal input support |

## Production olaylar

- **Whisper SIGABRT:** Native `.so` CLR static init'te crash — `Whisper.net.Runtime` kaldırıldı, voice DROP edildi.
- **Telegram 409:** 2 machine aynı anda long-poll → `fly scale count 1` ile çözüldü.
- **AmbiguityReason serializasyon hatası:** `List<string>` yerine `List<AmbiguityReason>` deserialization fix (`1ebbe94`).
- **Image E2E:** Kombi sigorta panosu fotoğrafı → başta Other/Low/Archive (yanlış) → prompt v2 sonrası → Electrical/High/AssignTechnician ✅

## Loom Q3 — güçlü aday (kesin giriyor)

> "Açık kablo fotoğrafı → Other/Low → prompt v2 sonrası → Electrical/High.
> Gerçek production senaryosu, gerçek düzeltme."

Görsel inputun sistemin sınırını nasıl ortaya çıkardığı — eval'de yoktu, production'da bulundu.

## Loom Q4 — 3 yeni hammadde

- Whisper native dep crash (ses input denemesi, CLR fast-fail)
- AmbiguityReason enum serializasyon hatası (int vs string)
- Telegram 409 multi-instance conflict (long-poll + scale)

## Yarın (Day 15)

- Decision Journalist kurulumu
- Gerçek mesajları izle, journal tut
- Edge case mining başlıyor
