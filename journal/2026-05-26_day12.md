## Day 12 — 26 May 2026 (Salı)

### Bugün yapılanlar

- Blok A tamamlandı: Dockerfile (multi-stage,
  ONNX RUN), fly.toml (fra, 8080, health check),
  ASPNETCORE_HTTP_PORTS blocker fix, SHA256,
  .dockerignore, MigrateAsync boot hook,
  README-deploy.md.

- Blok B tamamlandı: AnonymizationService
  (retention 6 ay, config-based).

- Blok C → Day 14'e ertelendi.

- Phase 2 DoD neredeyse tam — yalnızca
  KVKK disclosure ve deploy kaldı.

- origin/main → ed97188

### Beklemediğim problem / sürpriz

- ASPNETCORE_HTTP_PORTS eksikliği: Dockerfile'da
  EXPOSE 8080 vardı ama ASP.NET Core 8 default
  olarak 5000/5001 dinler. ENV eksik olsaydı
  Fly.io health check fail edecek, deploy
  başarısız olacaktı. Architect review'da tespit
  edildi, düzeltildi.

- MigrateAsync extension build error: Web projesi
  EF Core'u doğrudan reference etmiyor;
  using Microsoft.EntityFrameworkCore direktifi
  eksikti. Design package üzerinden referans
  gelmesine rağmen using gerekmekteydi.

- Embeddings__ModelPath path discrepancy:
  Direktifte "/app/models/model.onnx" yazıyordu.
  Gerçek path: "/app/models/multilingual-e5-small/model.onnx"
  (download-models.sh alt dizin oluşturuyor).
  README-deploy.md'de doğru path belgelendi.

### Aldığım karar + sebep

- AnonymizationService Infrastructure'a koyuldu
  (Application'da interface, Infrastructure'da impl):
  DbContext EF Core bağımlılığı Infrastructure'a ait.
  Directory path direktifle farklılaştı ama mimari
  doğruluk önceliklendirildi.

- MigrateAsync konumu: Hangfire registration
  sonrası, app.Run() öncesi. Exception → boot fail
  → Fly rollback. Beklenen davranış.

### Yarın (Day 13 — LIVE DEPLOY 🚀)

- ec-0003 + ec-0007 smoke test (önce)
- fly secrets set + fly deploy
- KVKK disclosure gönderimi
